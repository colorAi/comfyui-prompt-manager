I will update the CSS in `js/prompt_collector.js` to change the text color of the category count.

**File:** `js/prompt_collector.js`

**Change:**
Update the `.pm-category-count` CSS class:

```css
.pm-category-count {
    font-size: 11px;
    color: #fff; /* Changed from #666 to #fff for better visibility */
    background: #2a2a2a;
    padding: 2px 6px;
    border-radius: 10px;
    margin-right: 8px;
    min-width: 20px;
    text-align: center;
}
```

